name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: '1.23'

      - name: Vet
        working-directory: server
        run: go vet ./...

      - name: Lint
        uses: golangci/golangci-lint-action@v7
        with:
          version: v2.11.3
          working-directory: server

      - name: Test
        working-directory: server
        run: go test -race -count=1 ./...

      - name: Build
        working-directory: server
        run: CGO_ENABLED=0 go build -o ./bin/tma1-server ./cmd/tma1-server

      - name: Cross-compile Windows
        working-directory: server
        env:
          CGO_ENABLED: '0'
          GOOS: windows
          GOARCH: amd64
        run: go build -o /dev/null ./cmd/tma1-server

      - name: Lint JS
        working-directory: server/web
        run: npm ci && npx eslint js/

      - name: Lint install.sh
        run: shellcheck site/public/install.sh

      - name: Lint hook script (bash)
        run: |
          # Template uses {{PORT}} placeholder — replace for shellcheck validation.
          sed 's/{{PORT}}/14318/g' server/internal/hooks/tma1-hook.sh.tmpl > /tmp/tma1-hook.sh
          shellcheck /tmp/tma1-hook.sh

      - name: Lint install.ps1 + hook script (PowerShell)
        shell: pwsh
        run: |
          Install-Module -Name PSScriptAnalyzer -Force -Scope CurrentUser
          Invoke-ScriptAnalyzer -Path site/public/install.ps1 -EnableExit -Severity Error
          # Template uses {{PORT}} placeholder — replace for PSScriptAnalyzer validation.
          (Get-Content server/internal/hooks/tma1-hook.ps1.tmpl) -replace '\{\{PORT\}\}', '14318' | Set-Content $env:TEMP/tma1-hook.ps1
          Invoke-ScriptAnalyzer -Path $env:TEMP/tma1-hook.ps1 -EnableExit -Severity Error
